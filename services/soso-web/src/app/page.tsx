'use clinent'

export default function Page() {
    const handleCardClick = (cardId: string) => {
        console.log('Card clicked:', cardId);
        // カードクリック時の処理をここに実装
    };

    return (
      <div>Hi</div>

    )
}